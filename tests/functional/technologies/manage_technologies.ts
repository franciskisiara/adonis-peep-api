import { test } from '@japa/runner'

test('an authenticated user with a superuser account can create a technology', async ({ client }) => {
})

test('an authenticated user can view a list of configured technologies', async ({ client }) => {
})
